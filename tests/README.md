# Tests for exif2findertags

Tests require pytest be installed:

`python3 -m pip install pytest`

Tests also require that [exiftool](https://exiftool.org/) be installed.

After installing pytest and exiftool, run pytest:

`pytest`
